@model IEnumerable<QLKhoHang.Models.PhieuXuat>

@{
    ViewData["Title"] = "Danh sách Phiếu Xuất";
}

<div class="page-header fade-in">
    <h2><i class="bi bi-arrow-up-square"></i> Danh sách Phiếu Xuất</h2>
    <p class="subtitle">Quản lý các phiếu xuất hàng từ kho</p>
</div>

<div class="card-glass metric-card">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h5 class="mb-0 fw-bold">Phiếu xuất</h5>
            <div class="text-muted small">Tổng số: @Model.Count() phiếu</div>
        </div>
        <a asp-action="Create" class="btn btn-primary btn-modern">
            <i class="bi bi-plus-circle me-2"></i>Tạo Phiếu Xuất
        </a>
    </div>

    <div class="table-responsive">
        <table class="table-modern table table-hover mb-0">
            <thead>
                <tr>
                    <th class="text-center">Mã PX</th>
                    <th>Ngày xuất</th>
                    <th>Nhân viên</th>
                    <th>Khách hàng</th>
                    <th class="text-center" width="200">Thao tác</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Any())
                {
                    @foreach (var px in Model.OrderByDescending(x => x.NgayXuat))
                    {
                        <tr>
                            <td class="text-center">
                                <span class="badge bg-danger">@px.MaPX</span>
                            </td>
                            <td>
                                <i class="bi bi-calendar3 text-muted"></i>
                                @px.NgayXuat.ToString("dd/MM/yyyy")
                            </td>
                            <td>
                                <i class="bi bi-person text-muted"></i>
                                @px.NhanVien?.TenNV
                            </td>
                            <td>
                                <i class="bi bi-person-badge text-muted"></i>
                                @px.KhachHang?.TenKH
                            </td>
                            <td class="text-center">
                                <div class="btn-group">
                                    <a asp-action="Details" asp-route-id="@px.MaPX" 
                                       class="btn btn-sm btn-info btn-animated" title="Chi tiết">
                                        <i class="bi bi-eye"></i>
                                    </a>
                                    <a asp-action="Edit" asp-route-id="@px.MaPX" 
                                       class="btn btn-sm btn-warning btn-animated" title="Sửa">
                                        <i class="bi bi-pencil"></i>
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@px.MaPX" 
                                       class="btn btn-sm btn-danger btn-animated" title="Xóa"
                                       onclick="return confirm('Bạn có chắc muốn xóa @px.MaPX không?');">
                                        <i class="bi bi-trash"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="5" class="empty-state">
                            <i class="bi bi-inbox"></i>
                            <p class="mt-3">Chưa có phiếu xuất nào</p>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
